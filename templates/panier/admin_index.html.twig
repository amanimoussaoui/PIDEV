{% extends 'BackEnd/back.html.twig' %}

{% block title %}Gestion des Paniers{% endblock %}

{% block content %}
<style>
    /* CONTAINER PRINCIPAL */
    .container {
        max-width: 1000px;
        margin: 30px auto;
        padding: 20px;
        background: #fff;
        border-radius: 10px;
        box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.1);
    }

    /* TITRE */
    h2 {
        text-align: center;
        color: #2c7b2c;
        margin-bottom: 20px;
        font-size: 28px;
        font-weight: bold;
    }

    /* TABLEAU */
    .table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 20px;
    }

    .table th, .table td {
        padding: 15px;
        text-align: center;
        border-bottom: 1px solid #ddd;
    }

    .table th {
        background: #2c7b2c;
        color: white;
        text-transform: uppercase;
        font-size: 14px;
    }

    .table tr:hover {
        background: rgba(44, 123, 44, 0.1);
        transition: background 0.3s ease;
    }

    /* BOUTONS */
    .btn-container {
        display: flex;
        gap: 10px;
        justify-content: center;
    }

    .btn-delete {
        padding: 10px 16px;
        border-radius: 20px;
        text-decoration: none;
        font-size: 14px;
        transition: all 0.3s ease-in-out;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        font-weight: bold;
        border: none;
        background: #d9534f;
        color: white;
    }

    .btn-delete:hover {
        background: #c9302c;
        transform: scale(1.05);
    }

    /* ICÔNES */
    .btn-delete i {
        margin-right: 5px;
    }

    /* CENTRER LE TEXTE LORSQU'IL N'Y A PAS DE PRODUIT */
    .text-center {
        text-align: center;
        font-style: italic;
        color: #888;
        padding: 15px;
    }

    /* IMAGES */
    .table img {
        width: 70px;
        height: 70px;
        object-fit: cover;
        border-radius: 8px;
        transition: transform 0.3s;
    }

    .table img:hover {
        transform: scale(1.1);
    }

    /* ICÔNES DE TRI */
    .sorting-buttons {
        display: flex;
        justify-content: flex-end; /* Aligne les boutons à droite */
        gap: 20px;
        margin-top: 20px;
    }

    .sort-icon {
        padding: 10px 20px;
        border-radius: 20px;
        background: #f8f9fa;
        color: #2c7b2c;
        text-decoration: none;
        font-weight: bold;
        display: inline-flex;
        align-items: center;
        gap: 10px;
        transition: all 0.3s ease-in-out;
    }

    .sort-icon:hover {
        background: #2c7b2c;
        color: white;
        transform: scale(1.05);
    }

    .sort-icon.active {
        background: #2c7b2c;
        color: white;
    }

    .sort-icon i {
        font-size: 16px;
    }

</style>

<div class="container">
    <h2>Gestion des Paniers</h2>

    <a href="{{ path('panier_pdf') }}" title="Télécharger en PDF">
        <i class="fas fa-file-pdf"></i> Télécharger en PDF
    </a>
 
    <!-- Conteneur avec icône et texte à droite -->
    <div class="stats-container">
        <a href="{{ path('panier_dashboard') }}" class="stats-icon">
            <i class="fas fa-chart-bar"></i>
        </a>
        <a href="{{ path('panier_dashboard') }}" class="stats-text">Voir les Statistiques </a>  
    </div>

    <!-- Icônes de tri -->
    <div class="sorting-buttons">
        <a href="{{ path('panier_trier', {'ordre': 'asc'}) }}" class="sort-icon {% if app.request.query.get('ordre') == 'asc' %}active{% endif %}">
            <i class="fas fa-sort-amount-up"></i> Croissant
        </a>
        <a href="{{ path('panier_trier', {'ordre': 'desc'}) }}" class="sort-icon {% if app.request.query.get('ordre') == 'desc' %}active{% endif %}">
            <i class="fas fa-sort-amount-down"></i> Décroissant
        </a>
    </div>

    {% for message in app.flashes('success') %}
        <div class="alert alert-success">{{ message }}</div>
    {% endfor %}

    {% for message in app.flashes('error') %}
        <div class="alert alert-danger">{{ message }}</div>
    {% endfor %}

    <table class="table">
        <thead>
            <tr>
                <th>Image</th>
                <th>Produit</th>
                <th>Quantité</th>
                <th>Prix Total</th>
                <th>Agriculteur</th>
                <th>Actions</th>
            </tr>
        </thead>
       
        <tbody>
            {% for panier in paniers %}
            <tr>
                <td>
                    {% if panier.product is not null and panier.product.image is not null %}
                        <img src="{{ asset('uploads/images/' ~ panier.product.image) }}" alt="Image du produit" class="card-img-top">
                    {% else %}
                        <span>Aucune image</span>
                    {% endif %}
                </td>
                <td>
                    {% if panier.product is not null %}
                        {{ panier.product.nom }}
                    {% else %}
                        Produit non trouvé
                    {% endif %}
                </td>
                <td>{{ panier.quantite }}</td>
                <td>{{ panier.totale }} DT</td>
                <td>{{ panier.product.utilisateurs.nom }}</td>
                <td class="btn-container">
                    <form method="post" action="{{ path('panier_admin_supprimer', {'id': panier.id}) }}">
                        <button type="submit" class="btn-delete">
                            <i class="fas fa-trash"></i> Supprimer
                        </button>
                    </form>
                </td>
            </tr>
            {% else %}
                <tr>
                    <td colspan="5" class="text-center">Aucun panier disponible.</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
